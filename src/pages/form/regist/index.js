import React from 'react'
import { Card, Form, Button, Input, Checkbox, Radio, Select, Switch, DatePicker, TimerPicker, Upload, message  } from 'antd'
import './index.less'

export default class FormRegist extends React.Component {
    render () {
        return (
            <div>

            </div>
        )
    }
}